# Snug — WebScraper
#
# Run stages independently from the project root:
#
#   Stage 1 — product_discovery (collect product URLs):
#       python -m product_discovery.scraper
#
#   Stage 2 — page_search (scrape size charts):
#       python -m page_search.run
